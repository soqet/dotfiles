# colorization
alias ls='ls --color=auto'
alias dir='dir --color=auto'
alias vdir='vdir --color=auto'

alias ll='ls -la'

alias nv='nvim .'

# docker start-stop
alias docker-start='systemctl start docker.socket'
alias docker-stop='systemctl stop docker.socket'

# purge
alias purge='yay -Rnsuv'
